# Archive: Migrate interrupts to `wiringPiISR2` via a self-pipe

Completed tasks split out of isr-migration.md to keep the active plan lean. See
that file for the live Validation Table, rules and tracking.

## Archived V Tasks

- V1: Prereq gate — confirm installed `<wiringPi.h>` declares `wiringPiISR2` / `wiringPiISRStop` / `struct WPIWfiStatus` (≥ 3.16) — ✅ 2026-06-04 attempt 1: PASS. Grepped `/usr/local/include/wiringPi.h` (the installed 3.18 header on `rpi1`; `/usr/include/wiringPi.h` is absent): `struct WPIWfiStatus` at :298, `wiringPiISR2` at :308 (tagged `// V3.16`), `wiringPiISRStop` at :309 (`//V3.2`). The `wiringPiISR2` prototype confirms the `void* userdata` parameter the self-pipe design keys on: `int wiringPiISR2(int pin, int edgeMode, void (*function)(struct WPIWfiStatus wfiStatus, void* userdata), unsigned long debounce_period_us, void* userdata)`. (`waitForInterrupt2` at :307 and the legacy `waitForInterruptClose` alias at :310 also present; per plan we wrap only `wiringPiISRStop`.) Precheck only — no code change.

- V2: Edge validation + constants (F23) — ✅ 2026-06-04 attempt 1: PASS. Added the four `INT_EDGE_*` constants (`use constant`: SETUP=0, FALLING=1, RISING=2, BOTH=3 — mirror wiringPi's `#define`s) and exported them via `@wpi_constants` → `:constants`/`:all`. Added ordered validation to the Perl `set_interrupt($pin, $edge, $callback)`: `$pin` positive integer, then `$edge` ∈ {1,2,3} (rejects SETUP=0 and junk — SETUP is a setup-only mode, not a trigger), then `$callback` a CODE ref; each croaks with a named message. (Validation placed in the Perl layer, not the soon-to-be-replaced XS `setInterrupt`; the XS keeps its own pin/callback checks as a backstop until V4.) `perl -c` OK; built clean on `rpi1` (no warnings); functional check: constants import as 0/1/2/3; edges 0/4/"x"/undef/1.5/-1 all rejected, bad pin/callback rejected in order. Changes entry deferred to V8 per this plan's maintenance model (V8 updates `3.1801 UNREL`). Discovery logged: examples/POD use `EDGE_RISING` vs the `INT_EDGE_*` chosen here — reconcile in V7/V8.

- V3: Wrap `wiringPiISRStop(int pin)` — ✅ 2026-06-04 attempt 1: PASS. Added a thin XS wrap (`int wiringPiISRStop(pin) / int pin`) in the `# custom` interrupt section right after `setInterrupt` (API.xs), and added `wiringPiISRStop` to `@wpi_c_functions` (→ `:wiringPi`/`:all`). Only `wiringPiISRStop` wrapped — the legacy `waitForInterruptClose` alias is left out per plan. XS `process_file` parses; `perl -c` OK; built clean on `rpi1` (no warnings → symbol resolves against libwiringPi under default linking). Verified: imports + present as an XS sub in WiringPi::API + exported via `:wiringPi`; calling it with no `setup()` reaches wiringPi's own "not initialised" abort (proves the real symbol is invoked). Changes entry deferred to V8 per this plan's model. Needed for V4 re-arm + V6 teardown.

- V4: Self-pipe core (F12/F10/F22) + remove dead initThread — ✅ 2026-06-04 attempt 1: PASS. **Added** (API.xs C section): `isr_event_t {int pin; int edge; long long ts_us;}`; lazily-created non-blocking self-pipe `interrupt_pipe[2]`; `interrupts_dropped` counter (F24, atomic `__sync_fetch_and_add`); `isr2_writer(WPIWfiStatus, userdata)` — async-safe, pin from `userdata` (NOT `wfiStatus.pinBCM`), `write()`s the record, bumps the drop counter on a short/failed write, never touches Perl; `ensure_interrupt_pipe()`; `_arm_interrupt(pin, edge, debounce)` — bounds-checks pin, creates the pipe, **unconditionally `wiringPiISRStop(pin)` then `wiringPiISR2(pin, edge, isr2_writer, debounce, (void*)(intptr_t)pin)`** (the stop-first kills F25 stacked listeners; confirmed safe on an unarmed pin — wiringPiISRStop only aborts pre-`setup()`, else no-ops); `interrupt_fd()` (read end, -1 until first arm). New XSUBs `_arm_interrupt`/`interrupt_fd`; added `#include <unistd.h>`/`<fcntl.h>`. **Deleted**: the 40 generated `interruptHandler_*` trampolines + `MAKE_HANDLER`/`APPLY_TO_PINS`/`HANDLER_PTR`/`interrupt_handlers[]`; `mine`; `perl_callbacks[]`; the `event_queue`+mutex+cond+dispatcher thread + `ISR_enqueue/dequeue/dispatcher_main`; the XS `setInterrupt` (C fn + XSUB) and its `@wpi_c_functions` export + API.h decl; and (user-authorized, resolves the shared-`mine` entanglement) the dead unexposed `initThread` + `thread_callback_sv` + `PI_THREAD(myThread)` + API.h decl — **this completes threads-patch.md V1's deletion early**. Perl: `set_interrupt` now arms via `_arm_interrupt($pin,$edge,0)` (callback-registry + dispatch land in V5 — noted in a code comment); `interrupt_fd` exported via `@wpi_perl_functions`. XS parses; `perl -c` OK; residue grep empty; clean `realclean`+build on `rpi1` (no warnings; `wiringPiISR2`/`wiringPiISRStop` resolve); `make test` PASS (`.so` loads under bind-now — F22 check); verified `interrupt_fd` importable + returns -1 pre-arm, `_arm_interrupt` present, `setInterrupt`/`initThread` gone. Changes deferred to V8. **Note for threads-patch.md:** its V1 is now done here — mark it accordingly when that plan resumes.

- V5: Perl-side dispatch — ✅ 2026-06-04 attempt 1: PASS. `set_interrupt($pin,$edge,$cb)` now stores `$cb` in the per-interpreter file-lexical `%_interrupt_cb` (keyed by the user's pin) before `_arm_interrupt`. Added `dispatch_interrupts()` — non-blocking drain: reads 16-byte records from a cached dup (`<&`) of `interrupt_fd()`, `unpack "i i q"` → `{pin,edge,ts_us}`, calls `$_interrupt_cb{$pin}->($edge,$ts_us)`; drain contract honored (`undef`+EINTR→retry, else stop; `n==0` EOF→stop; never busy-spins on EAGAIN) — returns the dispatched count. Added `wait_interrupts($timeout_ms)` — 4-arg `select` on the fd (ms→s; undef=block) then dispatch. Added XS `interrupt_dropped()` accessor (+ C accessor) returning the `interrupts_dropped` counter (F24). New private `_interrupt_fh` caches the dup keyed on fd (re-opens if the pipe is torn down + re-armed; dup means closing it never closes the C-side fd). Exports: `dispatch_interrupts`/`wait_interrupts`/`interrupt_dropped` added to `@wpi_perl_functions` (→ `:perl`/`:all`). XS parses; `perl -c` OK; clean build (no warnings); **hardware-free end-to-end test** (inject `pack "i i q"` records into a non-blocking pipe, stub `interrupt_fd`/`_arm_interrupt`): 3 records routed to the correct pin-7/pin-9 callbacks with correct edge+ts, second drain returns 0 (no spin), `wait_interrupts(500)` select+dispatch works, `interrupt_dropped`=0; `make test` PASS under bind-now. Changes updated (kept current per user). Callback registry in Perl = F17/F18/F19 stay dissolved.

- V6: Teardown — ✅ 2026-06-04 attempt 1: PASS. Added `stop_interrupt($pin)` (validates pin, `wiringPiISRStop($pin)` + `delete $_interrupt_cb{$pin}`) and `stop_interrupts()` (stops every armed pin via the registry keys, closes the cached read dup `$_interrupt_fh` + nulls `$_interrupt_fh_fd`, then the new XS `_close_interrupt_pipe()` closes `interrupt_pipe[0..1]`, resets them to -1 and zeroes `interrupts_dropped`). No thread to join (the design win). A later `set_interrupt()` lazily re-creates the pipe + dup. Exports `stop_interrupt`/`stop_interrupts` added to `@wpi_perl_functions` (→ `:perl`/`:all`); `_close_interrupt_pipe` stays internal. XS parses; `perl -c` OK; clean build (no warnings); **hardware-free teardown test** (stub `wiringPiISRStop`/`_arm_interrupt`/`interrupt_fd`): both pins fire, `stop_interrupt(7)` then leaves only pin-9 firing, `stop_interrupts()` then fires nothing (registry cleared), `interrupt_dropped`=0; `make test` PASS under bind-now. Changes updated. **Note for B8:** `stop_interrupts()` now exists — RPi::WiringPi cleanup integration can be assessed.

- V7: POD + concurrency contract — ✅ 2026-06-04 attempt 1: PASS. Rewrote the INTERRUPT FUNCTIONS POD for the self-pipe model: documented `set_interrupt` (now with the optional 4th `$debounce_us`), `dispatch_interrupts`/`wait_interrupts`/`interrupt_fd`/`interrupt_dropped`/`stop_interrupt(s)`, the `INT_EDGE_*` constants, and **two worked examples** (single-threaded event loop on any Perl; background via `fork`) — and deleted the stale "needs a threaded Perl or it segfaults" caveat. Code: `set_interrupt` gained the optional `$debounce_us` (validated non-negative int, default 0, passed to `_arm_interrupt`); switched `set_interrupt`/`wait_interrupts` OO-detection from `shift if @_ == N` to `shift if @_ && ref $_[0]` (the optional args made the count ambiguous; `$pin`/`$timeout` are never refs). Reconciled the `EDGE_RISING`→`INT_EDGE_RISING` naming in this plan's embedded examples (`docs/interrupt-examples.md` already correct) — closes the V2 Discovery item. **Folded in UPGRADE-3.18 B8**: fixed the `=head1 ADC FUNCTIONS` leading space + `L</digitalReadByte()>` link + 3 whitespace lines, so `podchecker` is now fully clean (was 2 errors/3 warnings). `perl -c` OK; clean build (no warnings); functional test: debounce flows through for function+method, with/without (0/1500/0/250), bad debounce croaks; `make test` PASS. Changes updated. Remaining: V8 (Pi full gate + exercisers), V9 (downstream).

- V8: Full gate (Pi) — ✅ 2026-06-04 attempt 1: PASS. Clean `realclean`+`perl Makefile.PL`+`make`+`make test` on `rpi1` (Pi 5 / RP1). **Real-hardware exercisers** (edges generated by toggling BCM17's internal pull resistor — electrically safe, BCM17 floating): (a) single-threaded `setup_gpio()` BCM17 → 5 pull cycles = 10 edges in FALLING/RISING order, `interrupt_fd` -1→fd→-1; (b) `setup()` wpi-0 (= BCM17) → 10 edges, proving **userdata keying (F12)** — callback for wpi pin 0 fires, not mis-keyed to pinBCM 17; (c) re-arm twice → still 10 (no stacked listener, **F25**); (d) `stop_interrupt`/`stop_interrupts` → no further edges; (e) **fork background** → child arms+`wait_interrupts` loop dispatches edges the parent drives. All codified in `t/75-interrupts.t` (`PI_BOARD=1 prove` = 22 PASS). **valgrind** (installed 3.24.0) `--leak-check=full` on arm/drive/re-arm/teardown: **definitely lost 0, indirectly lost 0**; no memory-error or leak trace references any module symbol (`isr2_writer`/`_arm_interrupt`/`dispatch`/`_close_interrupt_pipe`); our self-pipe fds closed at teardown (only libwiringPi's own `/dev/gpiochip0`+`/dev/gpiomem0` remain). Remaining valgrind noise (possibly-lost 1.1MB = wiringPi per-pin ISR thread stacks; ~185 "uninitialised"/fd reports) is all inside libwiringPi (mmap hardware-register reads + gpiod threads) — intrinsic to running wiringPi under valgrind, not introduced here. Changes updated. **Interrupt migration functionally complete; only V9 (downstream RPi::WiringPi) remains.**

- V9: Downstream gate (RPi::Pin + RPi::WiringPi) — ✅ 2026-06-05 attempt 2: PASS. Fixed downstream (per 2026-06-04 decision to keep WiringPi::API as-is) and re-ran on real Pi 5 hardware: all three interrupt suites' interrupt assertions pass — `t/200` rising ×3, `t/201` falling ×3, `t/202` both 2/4/6 — under the new explicit-dispatch model. **RPi::Pin** (`~/repos/rpi-pin`): `set_interrupt($edge,$cb,$debounce_us)` now requires a CODE ref (string handler dropped, backwards-incompatible per user), validates `$edge`/`$callback`/`$debounce_us` in order, passes debounce through; `interrupt_set` now delegates correctly (it had been passing the pin number as the edge); SYNOPSIS + POD updated. **RPi::WiringPi** (`~/repos/rpi-wiringpi`): added `wait_interrupts`/`dispatch_interrupts`/`stop_interrupts` proxy methods; `RPi::WiringPi::Core::cleanup` now calls `WiringPi::API::stop_interrupts()` (**B8 done** — `_close_interrupt_pipe` is guarded so it's a safe no-op when nothing armed); FAQ "Interrupt usage" + new INTERRUPT METHODS POD section rewritten for coderef + dispatch; `t/200-202` arm with `\&handler` and drive `$pi->wait_interrupts(500)` before each assert; `defacto_interrupt.pl` updated to coderef + a `wait_interrupts` poll loop; Changes entries added in both repos. No wiringpi-api consumer-visible change (downstream-only) → no wiringpi-api Changes entry. Ran via `PERL5LIB` → wiringpi-api `blib` (bypasses installed-`.so` executable-stack error, V33). **Caveat (orthogonal, not V9):** the suites also run RPiTest `rpi_check_pin_status`, which fails because its hardcoded `rpi_default_pin_config` doesn't match this board's actual default alt-modes (pin 2 alt=0 vs 4; pins 14/15/23/24/25 alt=31) — reproduced with a bare `setup_gpio`+`get_alt`, no interrupts; a downstream/board-config issue outside this migration. Downstream repos not committed (working tree).

- V10: background_interrupt (hidden fork) — ✅ 2026-06-05 attempt 1: PASS. Added `background_interrupt($pin, $edge, $callback, $debounce_us)` to WiringPi::API: validates all args and croaks **before** forking; forks; the **child** installs a `SIGTERM` handler (runs `stop_interrupt($pin)` then `exit 0`), arms via `set_interrupt` (post-fork — wiringPi ISR pthreads don't survive fork) and runs `wait_interrupts(1000) while 1`; the **parent** records the child in a `WiringPi::API::BackgroundInterrupt` handle (in `@_bg_children`) and returns it. Handle: `pid`; `running` (reaps-if-exited via `waitpid WNOHANG`); `stop` (idempotent — TERM, poll 50×10ms, escalate KILL, `waitpid`); `DESTROY` stops a live child; module `END` block reaps any survivors so a forgotten `stop` can't leak a zombie. No global `$SIG{CHLD}`. Exported (`:perl`/`:all`). Hardware exerciser (BCM17 pull-resistor edges): background child handled ~9-10/10 edges, `running` true→false across `stop`, `stop` idempotent, pre-fork validation croaks on non-coderef/bad-edge. `make test` 207 PASS; POD + scenario-8 example added; Changes updated.

- V11: auto_dispatch_interrupts (SIGIO) — ✅ 2026-06-05 attempt 1: PASS. Added `auto_dispatch_interrupts($bool)` to WiringPi::API: on enable, saves `$SIG{IO}`, installs `\&dispatch_interrupts` as the handler, and (via `_auto_dispatch_apply`) puts the interrupt read fd into async mode — `fcntl F_SETOWN $$` + `F_SETFL |O_ASYNC` — then drains once; on disable, clears `O_ASYNC` and restores the prior handler. Wiring is lazy: enable before `set_interrupt` is fine — arming creates the pipe and `set_interrupt` calls `_auto_dispatch_apply` to wire the new fd; `stop_interrupts` resets the wired-fd marker so a re-arm re-wires. Idempotent enable/disable; validates the boolean arg. **Gotcha:** `fcntl(F_SETOWN, $$)` failed `ESRCH` because `$$` had been stringified elsewhere, so Perl's `fcntl` passed a string pointer (kernel read garbage) — fixed by forcing `my $owner = 0 + $$` and using `defined`-checks (F_GETFL can return 0). Exported (`:perl`/`:all`). Hardware exerciser: 10/10 edges auto-dispatched in the main process with **no** dispatch loop, none after disable; safe-signal callback updated a plain Perl variable (no locking). `make test` 207 PASS; POD + scenario-7 example added; Changes updated. **Discovery (→ V12):** RPi::WiringPi `cleanup`/`DESTROY` aren't fork-aware (the `{proc}` pid is stored but never checked), so OO exposure of the forking `background_interrupt` needs that guard first.

- V12: Downstream OO exposure of V10/V11 + fork-safe cleanup — ✅ 2026-06-05 attempt 1: PASS. **Fork-safety fix:** `RPi::WiringPi::Core::cleanup` now returns early when `$self->{proc} != $$` (the pid stored at construction but previously never checked), so a forked child sharing the object no longer resets pins, mutates the shared meta, or calls `stop_interrupts()` on the parent's behalf at exit (DESTROY routes through cleanup, so it's covered too). **OO wrappers:** added `$pi->auto_dispatch_interrupts($bool)` to RPi::WiringPi (process-global → belongs on the Pi object, not a pin) and `$pin->background_interrupt($edge,$cb,$debounce_us)` to RPi::Pin (pin-centric; validates then calls `WiringPi::API::background_interrupt($self->num,...)`). POD added for both, plus a "Hands-off handling" FAQ section and a note on `cleanup`'s fork behaviour; Changes updated in both repos. Hardware exerciser under RPi::WiringPi on Pi 5: a witness pin driven OUTPUT/HIGH **stayed** OUTPUT/HIGH after the background child exited (proves the guard); the background child handled ~9-10/10 edges with `running`/`stop` correct; `$pi->auto_dispatch_interrupts` fired 10/10 with no loop; the parent's own `cleanup` still ran. Non-fork path re-verified: `t/200` interrupt assertions still green. (`auto_dispatch_interrupts` deliberately NOT added to RPi::Pin — it's a per-process switch; a per-pin method would mislead.)

- V13: last_interrupt() stats accessor (promoted from B1) — ✅ 2026-06-05 attempt 1: PASS. Widened the self-pipe record `isr_event_t` (API.xs) from `{int pin; int edge; long long ts}` (16 bytes, `"i i q"`) to `{int pin; unsigned int pin_bcm; int edge; int status; long long ts}` (24 bytes, `"i I i i q"`), capturing `wfiStatus.pinBCM` and `wfiStatus.statusOK` that `isr2_writer` previously discarded. `dispatch_interrupts` now reads 24 bytes, unpacks all five fields, and publishes `$_last_interrupt = {pin, pin_bcm, edge, status, ts_us}` **before** invoking the callback. Added `last_interrupt()` (exported) returning a fresh copy of that hashref (or undef; reset by `stop_interrupts`) — so a callback, which still only gets `($edge, $ts_us)`, can recover the BCM pin/status (handy for one handler armed on many pins). Callback signature unchanged. Updated `t/75` (record format + last_interrupt coverage incl. undef-after-stop and copy isolation) and the examples doc's 16-byte references + API table. `make test` 209 PASS (hardware path included). Hardware: `last_interrupt()->{pin_bcm}` == 17 for BCM17, status == 1, undef before first/after stop, returned copy isolated. **Downstream:** exposed `$pi->last_interrupt` on RPi::WiringPi (process-wide, like `auto_dispatch_interrupts` → Pi object, NOT RPi::Pin), with POD + Changes; verified on hardware. B1 slot retired.

- V14: Overflow policy + interrupt_buffer() sizing (promoted from B2) — ✅ 2026-06-05 attempt 1: PASS. Documented the contract in the interrupt_dropped() POD: edges FIFO-queue in the kernel pipe (non-blocking writes from the ISR thread); on a full pipe the newest are dropped — never merged, never blocked — and counted, so loss is never silent (debounce is the only intentional edge-collapsing mechanism). Added interrupt_buffer([$bytes]) (exported): getter returns the live pipe capacity (F_GETPIPE_SZ), or the pending request if not yet armed; setter validates a positive integer, requests it via F_SETPIPE_SZ and returns the kernel-granted size (page-rounded, capped at /proc/sys/fs/pipe-max-size). The request is remembered and applied when the pipe is (re)created, so it can be set before arming and persists across stop_interrupts re-arm (mirrors auto_dispatch_interrupts; tracked by $_interrupt_buffer_req/$_interrupt_buffer_fd). t/75 gains hardware-free get/set/validation coverage; make test 214 PASS. Hardware: set 1 MiB before arm → live size 1048576 after arm; persisted across stop+re-arm; live round-trip 128 KiB → 131072. Downstream: $pi->interrupt_buffer on RPi::WiringPi (process-wide → Pi object, NOT RPi::Pin) + a "Bursts and dropped edges" FAQ note; POD + Changes; hardware-verified. B2 slot retired.

- V15: run_interrupt_loop()/stop_interrupt_loop() dispatch-loop helper (promoted from B3) — ✅ 2026-06-05 attempt 1: PASS. Added run_interrupt_loop($timeout_ms, $max) (exported): a blocking loop calling wait_interrupts($timeout_ms) (poll default 1000), returning the total dispatched; stops when stop_interrupt_loop() flips a flag (callback/signal-safe) or after $max events; validates positive-int args; sleeps the poll interval instead of busy-spinning when nothing is armed (wait_interrupts returns at once with no fd). Added stop_interrupt_loop() (exported). t/75 gains hardware-free coverage (max-stop, callback-stop, validation) — flushed out and fixed a test-only hang: the fake-pipe blocks must stop_interrupts() to drop the cached _interrupt_fh dup, else an fd-reuse ABA made wait_interrupts select on a stale EOF handle (not a production bug; real use clears the cache via stop_interrupts). make test 219 PASS. Hardware: run_interrupt_loop(200,5) returned exactly 5; stop_interrupt_loop() from a callback after 3 edges returned 3. Downstream: $pi->run_interrupt_loop / $pi->stop_interrupt_loop on RPi::WiringPi (process-wide → Pi object, NOT RPi::Pin), POD + Changes, hardware-verified (n=4 via callback stop). B3 slot retired.

- V16: background_interrupts() shared child for many pins (promoted from B4) — ✅ 2026-06-05 attempt 1: PASS. Added background_interrupts([$pin,$edge,$cb,$deb], ...) (exported): validates every spec before forking, then ONE child arms all pins and runs a dual-fd select loop over the interrupt fd + a parent→child control pipe. Returns a WiringPi::API::BackgroundInterrupts handle (subclass of BackgroundInterrupt, inheriting pid/running/stop/DESTROY) adding arm($pin)/disarm($pin) — line commands ("arm N"/"disarm N") the child applies via set_interrupt/stop_interrupt against a callback table fixed at fork (fork can't carry new code, so only the registered set can be toggled; arming an unregistered pin croaks). stop() closes the control pipe (child EOF-shutdown path) then TERM/reap. Child also tears down on control EOF. t/75 spec-validation coverage (hardware-free). make test 223 PASS. Hardware: one child (single pid) serviced pins 17 and 27 (7/8 edges); disarm(27) froze its count; arm(27) resumed it (8→16); arm(unregistered) croaked; stop reaped. Downstream: $pi->background_interrupts on RPi::WiringPi (spans pins → Pi object, not RPi::Pin), POD + Changes, hardware-verified. B4 slot retired.

- V17: background_interrupt data-back channel (promoted from B5) — ✅ 2026-06-05 attempt 1: PASS. background_interrupt() now accepts an optional trailing options hashref; {results => 1} sets up a pipe before forking, the child wraps the callback so a defined return value is length-framed (pack "N" + payload) up to the parent, and the BackgroundInterrupt handle gains read() (non-blocking: select(0) then read one frame, undef if none/EOF) and fh() (for select/IO::Select). Single writer + blocking write keeps frames intact. Default (no results) is unchanged — the common case stays a one-liner. make test 223 PASS (validation hardware-free; behaviour exercised on hardware). Hardware: handler returning "edge=$e" delivered ~7/8 values to the parent via read(); payloads correct; stop reaped. Downstream: $pin->background_interrupt now forwards the options hashref (RPi::Pin), so $pin->background_interrupt($edge,$cb,$deb,{results=>1}) works; POD + Changes; hardware-verified (5 values via the OO path). B5 slot retired.

- V20: auto_dispatch_interrupts refinements (promoted from B6) — ✅ 2026-06-05 attempt 1: PASS. (a) Configurable delivery signal: auto_dispatch_interrupts($enable, $signal) takes an optional signal name (default 'IO'/SIGIO; 'SIGUSR1' and 'USR1' both accepted), resolved to a number via Config and wired with F_SETSIG so O_ASYNC raises that signal instead of SIGIO (avoids clashes); the handler is installed/restored on the chosen $SIG name, and _auto_dispatch_clear resets F_SETSIG to 0. (b) Per-set_interrupt opt-in: set_interrupt() now accepts an optional trailing options hashref; {auto_dispatch => 1} (or a signal name) enables auto-dispatch as part of arming (honestly the process-wide switch, not per-pin). **Gotcha (again):** F_SETSIG got the signum as a string (Config split) → fcntl passed a pointer → EINVAL; fixed by forcing 0 + $signum (same class as the earlier F_SETOWN $$ bug). t/75 hardware-free coverage (bool + unknown-signal validation; enable-on-USR2 installs/restores $SIG{USR2}, leaves SIGIO alone). make test 230 PASS. Hardware: USR1 delivery dispatched 8/8 edges with SIGIO untouched and the handler restored on disable; set_interrupt({auto_dispatch=>1}) fired 8/8 with no loop; unknown signal croaks; default SIGIO still 8/8. Downstream: $pi->auto_dispatch_interrupts($bool,$sig) forwards the signal; $pin->set_interrupt() forwards the options hashref; POD + Changes; hardware-verified (USR1 + opt-in both 8/8). B6 slot retired.

- V18: Refresh docs/interrupt-examples.md + emit lib/INTERRUPTS.pod — ✅ 2026-06-05 attempt 1: PASS. Updated docs/interrupt-examples.md to "implemented and shipping": replaced the stale Status banner, de-provisionalized the API-table footnotes, folded the new capabilities into existing scenarios (run_interrupt_loop into 2, last_interrupt into 4, interrupt_buffer + overflow policy into 6, the auto_dispatch_interrupts signal arg + set_interrupt {auto_dispatch} opt-in into 7, the background_interrupt {results} channel into 8), added scenario 10 (background_interrupts shared child) + a decision-guide row, refreshed the API reference table (background_interrupts, run_interrupt_loop/stop, interrupt_buffer, results/auto_dispatch opts), and appended the code-flow section (Perl→API.pm→API.xs→wiringPi, with the 24-byte record). Created lib/INTERRUPTS.pod — a faithful POD rendition omitting the top Status section and the bottom "What changed vs" section, KEEPING the code-flow section; podchecker clean; added to MANIFEST + a Changes bullet. make test still PASS (manifest test skipped — Test::CheckManifest not installed).

- V19: lib/INTERRUPTS.md for rpi-wiringpi + rpi-pin — ✅ 2026-06-05 attempt 1: PASS. Wrote `rpi-wiringpi/lib/INTERRUPTS.md` (OO guide: arm on `$pin = $pi->pin(N)`, drive/control on `$pi`; quick start, driving dispatch, hands-off auto_dispatch incl. signal + opt-in, background_interrupt + results, background_interrupts shared child, last_interrupt, interrupt_buffer/overflow, fork-aware cleanup, method-reference tables split by object) and `rpi-pin/lib/INTERRUPTS.md` (pin perspective: the dispatch gotcha front-and-centre, coderef requirement/breaking change, set_interrupt incl. \%opts/auto_dispatch, background_interrupt + results, deprecated interrupt_set, and a pointer table to the `$pi` process-level methods). Each reflects only its own surface; NEITHER includes a code-flow section (wiringpi-api-only, per spec). Both added to their MANIFESTs + Changes. Interrupt migration is now fully complete — features (V1-V17, V20), downstream gate/OO (V9-V12 + proxies), all backlog (B1-B8), and docs (V18-V19).

## Archived Fixes

- B8: RPi::WiringPi cleanup → stop_interrupts() — ✅ RESOLVED 2026-06-05. The cleanup/DESTROY path resets pins but, before this, left an OO consumer's armed interrupts (wiringPi ISR thread + self-pipe fds) dangling at teardown. Resolved in three parts: (1) **V9** wired `WiringPi::API::stop_interrupts()` into `RPi::WiringPi::Core::cleanup` (Core.pm:239) — safe even when nothing is armed (`_close_interrupt_pipe` is guarded); (2) **V12** made cleanup/DESTROY fork-aware (`return if $self->{proc} != $$`), so a forked child can't trigger the parent's interrupt teardown on exit; (3) the `=head3 cleanup` POD now states that cleanup releases armed interrupts so the consumer needn't call `stop_interrupts` themselves. Verified across V9 (t/200-202 green, non-fork cleanup still resets pins) and V12 (witness pin survived a background child's exit). Cross-links: V9 downstream gate / UPGRADE-3.18 V33. B8 slot retired.

- B7: Interrupt unit-test suite — ✅ RESOLVED 2026-06-04 (user-requested). Created `t/75-interrupts.t` (+ MANIFEST). Hardware-free (always runs): `INT_EDGE_*` constant values; `set_interrupt` validation croaks (bad pin/edge=SETUP/edge-OOR/non-coderef/non-int-debounce); dispatch routing by faking the self-pipe boundary (`local`-stub `interrupt_fd`/`_arm_interrupt`/`wiringPiISRStop`, inject `pack "i i q"` records) — 3 records route to the right per-pin callbacks, second drain=0 (no busy-spin), `stop_interrupt`/`stop_interrupts` clear the registry; `interrupt_dropped()` accessor. On-Pi (gated `$ENV{PI_BOARD}`, each setup mode in a forked child): real BCM17 arm via the pull-resistor edge trick → 10 FALLING/RISING edges, re-arm-no-stack, `interrupt_fd` -1→fd→-1, `stop_interrupt` stops firing; `setup()` wpi-0 numbering → 10 edges (userdata keying, F12); fork background child dispatches edges the parent drives. `make test` PASS (runs hardware-free, GPIO skipped); `PI_BOARD=1 prove` PASS (22 tests). B7 slot retired.
