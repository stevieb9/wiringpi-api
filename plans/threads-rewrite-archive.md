# Archive: threads-rewrite.md

Completed V tasks and resolved fixes for the `worker()` concurrency rewrite.
See threads-rewrite.md for the active plan.

## Archived V Tasks

- V1: `worker()` core (fork-based) + `WiringPi::API::Worker` handle — ✅ 2026-06-05 attempt 1: PASS
- V2: shared (`$w->value`) + results (`$w->read`/`$w->fh`) channels, length-framed over inherited pipes — ✅ 2026-06-05 attempt 1: PASS
- V3: pacing — `{interval => $secs}` (helper paces the loop) + `{once => 1}` (run body once, child exits) — ✅ 2026-06-05 attempt 1: PASS
- V4: opt-in ithread mechanism (`{mechanism => 'thread'}`) + `pi_lock`/`pi_unlock` key validation — ✅ 2026-06-05 attempt 1: PASS
- V5: POD — "CONCURRENCY / BACKGROUND WORKERS" section documenting `worker()`, options, handle, `pi_lock`/`pi_unlock`, setup-once contract + one-liner — ✅ 2026-06-05 attempt 1: PASS
- V6: rewrote `docs/threads-examples.md` to lead with `worker()`, demoted raw fork/threads/Async to "under the hood", un-parked, cross-linked; added `lib/WORKERS.pod` — ✅ 2026-06-05 attempt 1: PASS
- V7: extended `t/85-worker.t` with a `PI_BOARD`-gated GPIO block (worker drives BCM17, parent observes both levels + reaping; `{shared}` sampler reads the pin back via `value()`); off-Pi assertions already present, GPIO block self-skips without hardware — ✅ 2026-06-05 attempt 1: PASS
- V8: hardware-verified `worker()` on a Pi 5 (wiringPi 3.18) — `make test` PASS (279 tests, GPIO block drove BCM17 for real); added `testing/valgrind_worker.pl` exerciser, valgrind shows definitely/indirectly lost 0 (all errors are libperl baseline, confirmed against a load-only baseline) and no zombies; `{mechanism=>'thread'}` not run (non-threaded Perl) — ✅ 2026-06-05 attempt 1: PASS
- V10: fixed the `digital_write`/`digital_read` doc bug (non-exported names) -> `write_pin`/`read_pin` in the `worker()` POD synopsis, `lib/WORKERS.pod` and `docs/threads-examples.md`; byte variants untouched; `perl -c` + `podchecker` clean — ✅ 2026-06-05 attempt 1: PASS
- V11: applied the same `digital_write`/`digital_read` -> `write_pin`/`read_pin` fix to `docs/interrupt-examples.md` (function-reference table); `lib/INTERRUPTS.pod` already clean — ✅ 2026-06-05 attempt 1: PASS
- V12: added `worker(\&body, \%opts)` proxy method to `rpi-wiringpi`'s `lib/RPi/WiringPi.pm` (beside `background_interrupts`): forwards to `WiringPi::API::worker()`, tracks the handle on `$self->{workers}`, no duplicated validation; `perl -c` clean — ✅ 2026-06-06 attempt 1: PASS
- V13: reaped workers in `rpi-wiringpi`'s `lib/RPi/WiringPi/Core.pm` `cleanup()` — idempotently `->stop` every `$self->{workers}` handle and clear the list, after the forked-child proc guard and beside `stop_interrupts()`; `perl -c` clean — ✅ 2026-06-06 attempt 1: PASS
- V14: added `=head3 worker(\&body, \%opts)` POD to `rpi-wiringpi`'s `lib/RPi/WiringPi.pm` documenting the handle, the `once`/`interval`/`shared`/`results`/`mechanism` options, cleanup/DESTROY auto-stop, and cross-refs to `WiringPi::API`, `WORKERS`, `THREADS`; `podchecker` clean — ✅ 2026-06-06 attempt 1: PASS
- V15: added `rpi-wiringpi` `t/213-worker.t` (and MANIFEST) mirroring this repo's `t/85-worker.t` — off-Pi proxy-returns-handle/propagated-validation-croaks/`stop` idempotency over a results channel/`cleanup` reaps a tracked worker, plus a `PI_BOARD`-gated GPIO block driving `$pi->worker` on BCM18 and confirming `cleanup` stops it; `prove` PASS (25 tests) both off-Pi and with `PI_BOARD=1` (GPIO block hardware-verified: worker drove HIGH/LOW, no zombie) — ✅ 2026-06-06 attempt 1: PASS
- V16: full Pi gate for `rpi-wiringpi` — `perl Makefile.PL && make` clean (Perl 5.42.0, non-threaded, so `{mechanism=>'thread'}` is out of scope as in V8); `make test` PASS (green; peripheral/board tests self-skip without the wired rig + sudo env). Added `build_testing/valgrind_worker.pl` (and MANIFEST), the OO sibling of Phase 1's `testing/valgrind_worker.pl`, covering distinct-pin workers, a `{shared}` sampler read by main, `{interval}`/`{once}` pacing, stop/reap, cleanup-driven stop, and forgotten-stop exit reaping. Under `valgrind --leak-check=full --show-leak-kinds=all`, the parent and every forked worker (incl. the forgotten-stop pids) report definitely lost 0 / indirectly lost 0; `possibly lost` (3,208,004 B / 4,063 blocks) and the 362-error count are byte-identical across all processes = libperl baseline, not worker-attributable. No zombies/leftover children. `t/213-worker.t` re-confirmed on hardware (`PI_BOARD=1`, 25 tests PASS). NOTE: the complete wired-peripheral + sudo CI run (LCD/ADC/DAC/etc.) is the brewbuild rig job and isn't reproducible on this box; those tests skip here — ✅ 2026-06-06 attempt 1: PASS
- V9: created `rpi-wiringpi`'s `lib/WORKERS.pod` (+MANIFEST) — the OO concurrency/background-worker guide, leading with `$pi->worker(...)` (scenarios 1-5 hands-off, 6 the `{mechanism=>'thread'}` opt-in calling `WiringPi::API::pi_lock`/`pi_unlock` directly, no OO proxy) and demoting raw `fork`/`threads->create` to "under the hood" (7-8); highlights object-owned lifecycle (`cleanup`/`DESTROY` auto-stops tracked workers) and the setup-once-in-main contract; mirrors the reference `wiringpi-api/lib/WORKERS.pod` structure/voice. **Named WORKERS (not THREADS) per user direction.** Also folded in the V17 (B3) cross-link: FAQ + the `worker()` method POD now point to `L<WORKERS>` (was `L<THREADS>`). `podchecker` clean on WORKERS.pod, RPi/WiringPi.pm, FAQ.pod; `make test` PASS. NOTE: the top-level `lib/WORKERS.pod` install-path clash with WiringPi::API's `lib/WORKERS.pod` was flagged and then resolved — see Archived Fixes (both files namespaced) — ✅ 2026-06-06 attempt 1: PASS
- V17 (promoted from B3): corrected the stale "system Perl does not use threads, which is required for interrupts to function" note in `rpi-wiringpi`'s `lib/RPi/WiringPi/FAQ.pod` "Perlbrew configuration" section — reframed perlbrew as recommended-not-required, and clarified neither interrupts (internal C thread) nor fork-based `worker()` need a threaded Perl (only the `{mechanism=>'thread'}` opt-in does); cross-linked existing `L<RPi::WiringPi>`/`L<WiringPi::API>`/`L</"Interrupt usage">`. The `L<THREADS>` cross-link is folded into V9 (target file doesn't exist yet — avoids a dangling link). `podchecker` clean — ✅ 2026-06-06 attempt 1: PASS

## Archived Fixes

- Fix (post-V9): namespaced `INTERRUPTS.pod` and restructured the `rpi-wiringpi` docs to mirror `wiringpi-api`. (1) `wiringpi-api`: `git mv lib/INTERRUPTS.pod` → `lib/WiringPi/API/INTERRUPTS.pod` (path now matches its `=head1 NAME WiringPi::API::INTERRUPTS`), fixing 6 previously-dangling `L<WiringPi::API::INTERRUPTS>` links in WORKERS.pod (`perldoc WiringPi::API::INTERRUPTS` now resolves); MANIFEST + Changes updated. No install collision existed (rpi ships `.md`), but the name/path mismatch broke the links. (2) `rpi-wiringpi`: `git mv lib/INTERRUPTS.md` → `docs/interrupt-examples.md`; added `lib/RPi/WiringPi/INTERRUPTS.pod` (`perldoc RPi::WiringPi::INTERRUPTS`, the POD form) and `docs/threads-examples.md` (markdown companion to WORKERS.pod), so both topics now have the same `docs/*.md` + `lib/.../*.pod` split as `wiringpi-api`; rewrote WORKERS.pod's interrupt links to `L<RPi::WiringPi::INTERRUPTS>`, fixed the moved md's relative links, MANIFEST + Changes updated. Both repos: `podchecker` clean, `manicheck` clean, `make` clean, `rpi-wiringpi` `make test` PASS. ✅ 2026-06-06
- Fix (post-V9): resolved the `WORKERS.pod` install-path clash (both dists shipped a top-level `lib/WORKERS.pod` → same `$sitelib/WORKERS.pod`). Namespaced both into their module trees so paths match their declared `=head1 NAME`: `wiringpi-api` → `lib/WiringPi/API/WORKERS.pod` (`WiringPi::API::WORKERS`), `rpi-wiringpi` → `lib/RPi/WiringPi/WORKERS.pod` (`RPi::WiringPi::WORKERS`). Updated both MANIFESTs; rewrote the OO `L<WORKERS>` links to `L<RPi::WiringPi::WORKERS>` (FAQ + worker() method POD) and the doc's plain `C<WORKERS>` mentions to `L<WiringPi::API::WORKERS>`; updated the `docs/threads-examples.md` path ref and both `Changes`. `podchecker` clean both repos; `wiringpi-api` `make` copies the pod to blib; `rpi-wiringpi` `make test` PASS. ✅ 2026-06-06
