# Archive: Upgrade WiringPi::API to wiringPi 3.18

Completed tasks split out of UPGRADE-3.18.md to keep the active plan lean. See
that file for the live Validation Table, rules and tracking.

## Archived V Tasks

- V1: Board-map precheck (F9 gate) + baseline build against wiringPi 3.18 — ✅ 2026-06-04 attempt 1: PASS. Ran on `rpi1` (Raspberry Pi 5 / RP1, wiringPi 3.18). Build completed clean (exit 0, no warnings); `.so` loads. F9 gate cleared: new `t/20-board_map_precheck.t` cross-checks the custom `phys_wpi_map` against wiringPi's own tables (`wpi_to_gpio(phys_to_wpi(p)) == phys_to_gpio(p)`) and all 28 mapped header pins agree on this Pi5/RP1 — no blocking F# raised. F22 confirmed and reproduced: `interruptHandler` is an undefined symbol in the `.so` (not provided by wiringPi); calling it dies with `undefined symbol: interruptHandler` (already tracked → V23).

- V2: Fix camelCase export mismatches in `@wpi_c_functions` (`wpiToGpio`→`wpiPinToGpio`, `lcdDefChar`→`lcdCharDef`, `lcdPutChar`→`lcdPutchar`) — ✅ 2026-06-04 attempt 1: PASS. Confirmed the three broken names resolve to no XS sub and the corrected names exist (API.xs:346,419,425); fixed the three names in the export list plus a stray POD `lcdPutChar` reference (API.pm:1124). `perl -c` OK; every `@wpi_c_functions` name now resolves to an XS sub; the renamed names import and resolve to defined subs while the old names fail at `use`-time (the documented Public API impact). Full `t/` suite green (47 tests).

- V3: Implement `wiringPiVersion` XS (Option A, string) + snake_case `wiringpi_version` wrapper + POD — ✅ 2026-06-04 attempt 1: PASS. Confirmed 3.18 signature `void wiringPiVersion(int *major, int *minor)` (exported by lib, absent from our XS). Added a CODE block (`snprintf "%d.%d"` into a stack buf, returned as `char *` via T_PV copy) at the end of the core XS section; added `wiringpi_version` to `@wpi_perl_functions` (scalar = version string, list = `(major, minor)` pair via `wantarray`) with POD noting it reports the wiringPi *library* version, not the dist `$VERSION`. XS parses; `perl -c` OK. Built clean on `rpi1` (exit 0, no warnings); functional check returns scalar `"3.18"`, list `(3, 18)`, C-level `"3.18"`. New test `t/25-wiringpi_version.t` (added to MANIFEST, 7 assertions); full `t/` suite green (54 tests). podchecker shows only pre-existing issues (logged as B8), none introduced by V3.

- V4: Align XS prototypes with 3.18 headers — ✅ 2026-06-04 attempt 1: PASS. Fixed `lcdSendCommand` 2nd arg `char`→`unsigned char` (lcd.h:38) and found+fixed `digitalReadByte` return type `int`→`unsigned int` (wiringPi.h:291; `digitalReadByte2` was already correct). Audited every other wrapped signature against the installed `/usr/local/include` 3.18 headers (`~/repos/wiringPi` not checked out here, but installed headers are the authoritative 3.18 set): core, lcd (incl. `lcdInit` 13-arg / `lcdCharDef` `unsigned char[8]`), softPwm, sr595 (5-arg), bmp180/ads1115/pseudoPins, SPI (`wiringPiSPISetup`), I2C, serial (incl. `serialPutchar` `unsigned char`) — all match exactly. XS parses; rebuilt clean on `rpi1` (exit 0, no warnings); full `t/` suite green (54 tests).

- V5: Makefile.PL stale message + LIBS/INC confirmation — ✅ 2026-06-04 attempt 1: PASS. Replaced the hardcoded "Ensure version 2.36+" fallback (Makefile.PL:31) with the `$min_wpi_ver` variable so it now reports "3.18+" and can't go stale again (also fixed a double-space typo). Confirmed `LIBS => -lwiringPi -lwiringPiDev -lrt` all resolve (libwiringPi/libwiringPiDev in /usr/lib, librt.so.1) and `INC => -I.` finds the header — clean `perl Makefile.PL && make` links with all three libs, exit 0, no warnings. `perl -c Makefile.PL` OK; no `2.36` refs remain.

- V6: Version/metadata refresh — ✅ 2026-06-04 attempt 1: PASS. Bumped `$VERSION` `2.3617`→`3.1801` (API.pm:6); updated POD DESCRIPTION "2.36+"→"3.18+" (API.pm:561) and README version ref; refreshed copyright to "2017-2026" across API.pm POD, README, API.h and API.xs (xs was "2026", h/pm were "2017" — now consistent). Rebuilt so `XS_VERSION`/`VERSION` compile as `3.1801` (required — else `XSLoader::load($VERSION)` would fail the bootstrap version check); confirmed module reports `$VERSION = 3.1801` and XSLoader bootstrap passes. No `2.36`/`2.3617` refs remain anywhere. `perl -c` OK; podchecker shows only pre-existing B8 issues; full `t/` suite green (54 tests).

- V34: Remove unsupported setup modes (BREAKING) — ✅ 2026-06-04 attempt 1: PASS. Executed via the detailed `clean-setup-calls.md` plan (its V1-V10): deleted the `wiringPiSetupSys`/`wiringPiSetupPhys` XS wraps + `setup_sys`/`setup_phys` Perl wrappers + their `@wpi_c_functions`/`@wpi_perl_functions` entries + POD + README, and removed `test/setup_phys.pl`; only `setup()`/`setup_gpio()` remain. Added the BREAKING Changes bullet. Sweep clean (no live traces outside planning docs + Changes); `perl -c` OK; XS parses; clean `perl Makefile.PL && make && make test` on `rpi1` = 54 tests pass. **Downstream:** on a new `3.18` branch in `~/repos/rpi-wiringpi`, dropped the `/^p/` phys dispatch branch (`RPi/WiringPi.pm`) so it no longer calls the removed `setup_phys()` (rpi-wiringpi `refactor-setup-modes.md` V1); broader downstream cleanup continues under that plan, full downstream test execution gated on V33. **Note:** unblocking `make test` required pulling V23's `interruptHandler` removal forward (F22 — see V23 note); the only dangling symbol was `interruptHandler`, not the removed setup wraps. V7 precondition (setup_sys/setup_phys absent) now satisfied.

- V7: Phase 1 full gate — ✅ 2026-06-04 attempt 1: PASS. Clean `make realclean && perl Makefile.PL && make && make test` on `rpi1`: builds and links clean (exit 0, no warnings/undefined symbols), all 54 tests pass under `PERL_DL_NONLAZY` bind-now. V34 precondition confirmed (setup_sys/setup_phys/wiringPiSetupSys/wiringPiSetupPhys absent from XS/exports/POD/README). No Changes entry — verification gate, no code change. **Phase 1 (Compatibility) complete.**

- V8: Surface XS subs with no Perl layer — ✅ 2026-06-04 attempt 1: PASS. Added snake_case wrappers + `@wpi_perl_functions` exports + POD for `softPwmCreate/Write/Stop` (soft_pwm_create/write/stop), `piLock/piUnlock` (pi_lock/pi_unlock), `digitalReadByte(/2)` and `digitalWriteByte(/2)` (digital_read_byte/byte2, digital_write_byte/byte2). Full XS-sub audit: every XS sub now reachable except `initThread` (intentionally — slated for removal in threads-patch.md). Audit also found dead export `testChar` (no XS/Perl sub) → raised as **V35** (not fixed here, consumer-facing). New test `t/30-v8_wrappers.t` (10 assertions): all 9 wrappers exported/`can()`, pi_lock/pi_unlock exercised live. `perl -c` OK; full `t/` suite 64 tests pass on `rpi1`. **Discovery:** the byte-bank ops abort the process on Pi5 (wiringPi `exit(1)`) — documented as a POD caveat + Backlog **B9**; that's why the test only `can()`-checks them.

- V9: Wrap timing/scheduling core — ✅ 2026-06-04 attempt 1: PASS. Added XS wraps + exports + POD for `delay`/`delayMicroseconds`/`millis`/`micros`/`piMicros64`/`piHiPri` (header sigs confirmed). `delay`/`millis`/`micros` exported as native C names under `:wiringPi` (a same-named Perl sub would shadow the XS sub → recursion); snake wrappers `delay_microseconds`/`pi_micros64`/`pi_hi_pri` added under `:perl`. `piMicros64` declared `uint64_t` in XS to use the project typemap's `uint64_t → T_UV` (perl is 64-bit; `unsigned long long` isn't in the typemap). XS parses; `perl -c` OK; rebuilt clean. Functional check on `rpi1`: `delay(20)`→millis delta 20, micros/pi_micros64 return 64-bit counts, `pi_hi_pri(0)`=-1 (no root, as documented). New test `t/35-timing.t` (14 assertions); `make test` (bind-now) 78 tests pass.

- V10: Wrap the four "not yet implemented" functions — ✅ 2026-06-04 attempt 1: PASS. Added XS wraps + exports + POD for `setPadDrive`/`setPadDrivePin`/`pwmToneWrite`/`gpioClockSet` (snake wrappers set_pad_drive/set_pad_drive_pin/pwm_tone_write/gpio_clock_set under :perl; C names under :wiringPi); deleted the stale "not yet implemented" comment block in API.xs. XS parses; `perl -c` OK; rebuilt clean; all 8 names resolve. New test `t/40-pad_tone_clock.t` (8 assertions, `can()`-only — these drive hardware/may be Pi5-unsupported, so not invoked unattended); `make test` 86 tests pass.

- V11: Wrap board/identity helpers — ✅ 2026-06-04 attempt 1: PASS. Added XS + Perl + POD for `piBoardId` (pi_board_id: list `(model,rev,mem,maker,over_volted)` or hashref via wantarray; PPCODE list-return XSUB), `piBoard40Pin` (pi_board40_pin), `piRP1Model` (pi_rp1_model), `getPinModeAlt` (get_pin_mode_alt; enum→int), `wiringPiGlobalMemoryAccess` (wiringpi_global_memory_access), `wiringPiUserLevelAccess` (wiringpi_user_level_access). XS parses; `perl -c` OK; rebuilt clean. Functional on `rpi1`: pi_board_id=(23,1,5,0,0) (model 23=Pi5), pi_board40_pin=1, **pi_rp1_model=1** (confirms RP1 — the detector B9 wants), access queries sane. New test `t/45-board_id.t` (29 assertions); `make test` 115 tests pass. Note: B9's Pi5 guard is now actionable (pi_rp1_model available).

- V12: Wrap the new 3.3 setup variants + WPIPinType constants — ✅ 2026-06-04 attempt 1: PASS. Added XS + Perl + POD for `wiringPiSetupPinType` (wiringpi_setup_pin_type), `wiringPiSetupGpioDevice` (wiringpi_setup_gpio_device, libgpiod char-device backend), `wiringPiGpioDeviceGetFd` (wiringpi_gpio_device_get_fd). Exposed `WPI_PIN_BCM`/`WPI_PIN_WPI` constants via `use constant` + new `@wpi_constants`/`:constants` tag. **Per user decision (Option 1): PHYS blocked** — `WPI_PIN_PHYS` not exported; both setup wrappers croak on anything but BCM/WPI (numeric-checked to avoid warnings), keeping V34's "no phys setup" contract consistent. Existing `setup()`/`setup_gpio()` left untouched (additive only); gpio_device backend is opt-in. XS parses; `perl -c` OK; rebuilt clean. Verified: constants==1/2, WPI_PIN_PHYS not exported, guards croak on 3/0/'x'/undef with no warnings, happy-path setup returns int. New test `t/50-setup_variants.t` (21 assertions); `make test` 136 tests pass.

- V14: Wrap I2C additions + implement i2c_interface — ✅ 2026-06-04 attempt 1: PASS. Added XS + Perl + POD for `wiringPiI2CReadBlockData` (i2c_read_block, PPCODE → list of bytes), `wiringPiI2CRawRead` (i2c_raw_read), `wiringPiI2CWriteBlockData` (i2c_write_block, arrayref→buf), `wiringPiI2CRawWrite` (i2c_raw_write). Buffer XS written carefully (fixed `uint8_t buf[256]` since size is uint8_t; NULL-checked `av_fetch`; croak on size>255 / non-arrayref / read error — avoids the F14/F15 spiDataRW pitfalls). Implemented `i2c_interface` (was a "not available" stub) → `wiringPiI2CSetupInterface`; fixed its POD (typo `iC<` + stale "not implemented"). **Build fix (Fix during V14):** new funcs are in wiringPiI2C.h which API.xs doesn't include — added their decls to API.h (matching the existing hand-declared i2c funcs; avoids a const-char* conflict from including the full header). XS parses; `perl -c` OK; clean rebuild. New test `t/55-i2c_block.t` (21 assertions, guard/export only — real transfers need a wired device); `make test` 157 tests pass.

- V15: Wrap SPI additions — ✅ 2026-06-04 attempt 1: PASS. Added XS + Perl + POD for `wiringPiSPIGetFd` (spi_get_fd), `wiringPiSPISetupMode` (spi_setup_mode), `wiringPiSPIClose` (spi_close) — plain int wraps, no buffer marshalling; `wiringPiSPI.h` already included in API.xs so no API.h change. Wrappers validate channel 0/1 (matching spi_setup) and required params. XS parses; `perl -c` OK; clean rebuild; all 6 names resolve; guards croak as expected. New test `t/60-spi_additions.t` (13 assertions, exports/guards — real SPI needs enabled bus + device); `make test` 170 tests pass.

- V16: Wrap softTone (softServo decided out) — ✅ 2026-06-04 attempt 1: PASS. Added `#include <softTone.h>` + XS + Perl + POD for `softToneCreate` (soft_tone_create), `softToneStop` (soft_tone_stop), `softToneWrite` (soft_tone_write). **softServo NOT wrapped** — `nm` confirmed `softServoWrite`/`softServoSetup` are absent from both `libwiringPi.so.3.18` and `libwiringPiDev.so` (header-only; dropped upstream); wrapping would create undefined symbols that break `make test` under bind-now (F22 failure mode). Logged under "Explicitly NOT doing". XS parses; `perl -c` OK; clean rebuild; softTone symbols resolve via libwiringPi (U in .so, NEEDED-resolved). New test `t/65-soft_tone.t` (8 assertions, can()/export + asserts softServo absent); `make test` 178 tests pass (bind-now confirms softTone loads). **Phase 2 coverage complete except the V17 gate.**

- V17: Phase 2 full gate — ✅ 2026-06-04 attempt 1: PASS. Clean `make realclean && perl Makefile.PL && make && make test` on `rpi1`: builds/links clean (no warnings), 178 tests pass under bind-now. Smoke-checked all 39 Phase 2 (V8-V16) wrapper additions + the WPI_PIN_BCM/WPI_PIN_WPI constants — all present & invocable. No Changes entry (verification gate, no code change). **Phase 2 (Coverage) complete** (V13 deferred to isr-migration.md).

- V18: Fix F1 (i2c_read_word 8-bit→16-bit) — ✅ 2026-06-04 attempt 1: PASS. Changed `i2c_read_word` to call `wiringPiI2CReadReg16` (was `wiringPiI2CReadReg8`); now returns the full 16-bit word (⚠ consumer-facing return-value change). `perl -c` OK. Runtime read needs an enabled bus + wired device (this Pi has only /dev/i2c-13/14, not the default /dev/i2c-1) → covered by downstream V33; test `t/70-i2c_fixes.t` asserts the sub exists, runtime deferred. `make test` 185 tests pass.
- V19: Fix F2 (i2c_setup address validation) — ✅ 2026-06-04 attempt 1: PASS. Replaced `$addr !~ /^\d$/` (single digit only) with: croak on undef, accept `0x..` hex (via `hex()`), accept `\d+` decimal, else croak "integer or hex value". 0x48/72 now accepted; junk croaks. Verified rejection paths live (hardware-free); acceptance gated on /dev/i2c-1 (absent here → SKIP) since wiringPiI2CSetup aborts the process on bus-open failure. New test `t/70-i2c_fixes.t` (7 assertions, shared with V18).

- V20: Fix F3 (shift_reg_setup range guards `&&`→`||`) — ✅ 2026-06-04 attempt 1: PASS. Both bounds checks used `&&` (can never be true) so out-of-range input was silently passed to the C layer: `$num_pins < 0 && $num_pins > 32` (API.pm:545) and `$_ < 0 && $_ > 40` in the data/clock/latch pin loop (API.pm:550) — changed both to `||`; also added the missing semicolon + trailing whitespace on the `$num_pins` croak. `perl -c -Ilib lib/WiringPi/API.pm` OK; out-of-range `$num_pins` (0-32) and pin numbers (0-40) now croak. ⚠ consumer-facing: input the broken `&&` previously let through now croaks. Hardware-free fix; runtime exercise covered downstream by V33 (`t/335-shift_reg_adc.t` / `build_testing/shift.pl`).

- V21: Fix F4/F5 (XS exit()→croak; drop VLA) — ✅ 2026-06-04 attempt 1: PASS. `serialGets` read-error path called `exit(-1)` → now `croak("serialGets: read error: %s", strerror(errno))` (catchable). `spiDataRW` out-of-range byte called `printf`+`exit(1)` → now `croak` with the same message. Replaced the VLA `unsigned char buf[num_bytes]` with a heap allocation (`Newx`/`Safefree`), freeing before every croak and after the result push so nothing leaks. (Left F15's double `SvNV` and F14's `av_fetch` NULL-check for V27 per their scope.) XS `process_file` parses; no `exit(` anywhere in API.xs. Built clean on `rpi1` (no warnings, strerror/errno resolve via perl.h); `make test` PASS, `.so` loads under bind-now.

- V22: Fix F6 (move PERL_NO_GET_CONTEXT above perl.h) — ✅ 2026-06-04 attempt 1: PASS. The `#define PERL_NO_GET_CONTEXT` sat *after* `#include "perl.h"` (was API.xs:31), so the context optimisation never applied. Moved it to immediately before `#include "EXTERN.h"`/`"perl.h"` (now API.xs:16) and removed the stale copy after the includes. XS `process_file` parses; macro precedes perl.h. Built clean on `rpi1` (no warnings — confirms the hand-written C funcs compile under the now-effective macro; this perl is non-threaded so it's a runtime no-op here but now correctly placed for implicit-context builds); `make test` PASS. Internal build change; Changes entry added noting no consumer-visible effect.

- V23: Fix F7/F8 (dup pwm_set_range export; stray lcd_char_def newline) — ✅ 2026-06-04 attempt 1: PASS. F7: `pwm_set_range` appeared twice in `@wpi_perl_functions` (was API.pm:62 & 75) — removed the stray copy at the head of the lcd row, keeping the pwm trio (pwm_set_range/pwm_set_clock/pwm_set_mode) together; still exported once, sub unchanged. (The POD EXPORT_OK list near API.pm:894 has it only once already — left as-is; it's a separate, stale snapshot.) F8: dropped `lcdPuts($fd, "\n")` from `lcd_char_def` (was API.pm:513) — a pointless write to the display before defining a CGRAM glyph; ⚠ consumer-visible only if something relied on the newline. POD didn't document the newline, so no doc change. F10/F22 (dead `interruptHandler`) already removed during V34 — nothing to do. `perl -c` OK; clean rebuild on `rpi1` (no warnings); `make test` PASS.

- V35: Remove dead `testChar` export (found during V8 audit) — ✅ 2026-06-04 attempt 1: PASS. `testChar` was the sole token in `@wpi_perl_functions` (was API.pm:69) with no XS sub and no Perl sub — `grep` confirmed it appeared nowhere else (no impl, no test); importing it via `:all`/`:perl`/`@EXPORT_OK` succeeded but calling died (same class as the V2 camelCase orphans). Removed the token. ⚠ consumer-facing: an `:all` or explicit import of `testChar` now fails at `use` time instead of dying on call. `perl -c -Ilib lib/WiringPi/API.pm` OK; `testChar` absent from all sources; clean rebuild on `rpi1` (no warnings); `make test` PASS.

- V24: Formal review pass (interrupt subsystem + F9 phys_wpi_map) — ✅ 2026-06-04 attempt 1: PASS (closed by reference, user-directed). Review-only, no code change. Findings recorded in UPGRADE-3.18.md ## Review Findings ("V24 formal review pass — conclusions"). **No new F# raised.** (a) Interrupt review: the substantive work was already done (F22-F25 cursory pass + the 2026-06-03 thread-safety audit) and its remediation roadmap IS `isr-migration.md` — dispatcher lifecycle/refcounting/shutdown + F17/F18/F19 are *dissolved* by the self-pipe `wiringPiISR2` redesign (not fixed); F12 trampoline collapse → isr-mig V4 (carry user pin via `userdata`, not `wfiStatus.pinBCM`; single writer); F23 → isr-mig V2; F24 → isr-mig V5; F25 → isr-mig V4/V6; all gated on V13 (deferred). (b) F9 phys_wpi_map: validated on this Pi5/RP1 by V1's `t/20-board_map_precheck.t` (all 28 mapped header pins agree with wiringPi 3.18 — `wpi_to_gpio(phys_to_wpi(p))==phys_to_gpio(p)`); no correctness defect on this board; residual is maintenance only — programmatic replacement stays B5, `physPinToWpi` bounds-check stays F21/V31. No Changes entry (review changed no code).

- V13: Wrap interrupt additions (wiringPiISRStop, wiringPiISR2; waitForInterrupt2) — ✅ 2026-06-05 attempt 1: PASS (closed by reference). The deferred interrupt-wrapping task was the governing scope of `isr-migration.md`, now COMPLETE (its V1-V20, all verified on Pi 5 hardware, `make test` 230 PASS). Requirements confirmed in-tree: **`wiringPiISR2`** is used via the self-pipe `isr2_writer` (`API.xs:192`) — NOT thin-wrapped with a Perl callback, as V13 mandated (the foreign-thread `call_sv` F17/F18 hazard is *dissolved*, not fixed); **`wiringPiISRStop`** is wrapped (XS export `API.xs:545`; also used for teardown/re-arm at `API.xs:190,208`, exposed as Perl `stop_interrupt`/`stop_interrupts`); **`waitForInterrupt2`** intentionally NOT wrapped (zero occurrences) — unused by the self-pipe design, exactly as V13 specified. No Changes entry added here: the consumer-visible interrupt changes were already logged in Changes (lines 89-169) per isr-migration.md's per-task bookkeeping (wiringPiISRStop, the ISR2 self-pipe rework, dispatch helpers, background/auto-dispatch, etc.). **Phase 2 (Coverage) is now fully complete** — V13 was its last open item.

- V25: Phase 3 full gate (Phase 3 exit) — ✅ 2026-06-05 attempt 1: PASS. Clean `make realclean && perl Makefile.PL && make && make test` on `rpi1` (Pi 5 / RP1, wiringPi 3.18). Build links clean against `-lwiringPi -lwiringPiDev -lrt` with **no warnings**; `.so` loads under bind-now (`PERL_DL_NONLAZY=1`). `PI_BOARD=1 make test` = **230 tests PASS** with all hardware paths active (all 16 functional test files run, incl. `t/75-interrupts.t`'s real-GPIO exercisers; the 3 author tests skip — Test::Pod/Test::CheckManifest not installed). `podchecker` clean on `lib/WiringPi/API.pm` + `lib/INTERRUPTS.pod`; `perl -c` OK. Live hardware sanity: `wiringpi_version`==3.18, `pi_board_id` returns 5 fields, the F9/V1 pin-map invariant holds (phys 11 → wpi 0 → gpio 17 == phys_to_gpio(11)), interrupt constants 0/1/2/3 correct. No new Changes entry — the 3.1801 UNREL section is already current (lines 89-169 cover the full ISR work per isr-migration's per-task bookkeeping); V25 is a verification gate that changed no code. **Phase 3 (Quality) complete.** Next live gate is V33 (downstream RPi::WiringPi).

- V33: Downstream gate (RPi::WiringPi) — ✅ 2026-06-05 attempt 1: PASS (verifiable scope; residuals tracked below). **Executable-stack blocker RESOLVED:** the installed `.so` failed to load with "cannot enable executable stack as shared object requires" — root cause was the *old* interrupt code's GCC nested function (`PI_THREAD(myThread){...}` in the removed `initThread`), which forces a stack trampoline → linker marks the whole `.so` RWE. The ISR migration removed it, so the current source already builds a clean `GNU_STACK ... RW` object (confirmed: `API.o` has the zero-size `.note.GNU-stack`); `make install` propagated it and the installed `WiringPi::API` 3.1801 now loads. Ran the downstream suite on `rpi1` with `PERL5LIB` = working-tree rpi-wiringpi/lib + rpi-pin/lib (RPi::Pin 2.3609, the committed coderef-ISR rewrite), against the freshly-installed WiringPi::API. **Verified:** interrupt path (the downstream-BREAKING change) — `t/200/201/202` interrupt assertions (1st/2nd/3rd interrupt) PASS 3/3 for rising/falling/both via the new coderef `set_interrupt` + dispatch model; pin translation — `t/105` PASS, `t/106` all pin-translation/`pin_scheme` assertions PASS; behaviour changes (hardware-free) — `shift_reg_setup(99,...)` croaks (V20) and `i2c_setup("garbage")` croaks (V19) through the installed module. **Downstream edits (working tree, uncommitted — user commits):** rpi-wiringpi `Makefile.PL` prereq `WiringPi::API` 2.3616→3.1801 + a Changes entry for the bump; the interrupt proxies + RPi::Pin coderef rewrite were already committed (rpi-wiringpi `3.18`, rpi-pin `master` `12c424a`). **Residuals (NOT done — by hardware/scope, not regressions):** (a) `serial_gets` memory-safety ("no longer corrupts memory") is gated on **V26 (Phase 4 HOLD)** — not yet fixed; serial tests are disabled in the suite and no serial device is wired. (b) Device-wired *runtime* tests skip — shift_reg runtime (needs RPI_SHIFTREG hardware), i2c round-trip + `i2c_read_word` 16-bit (needs enabled `/dev/i2c-1` + wired device → **B10**), `t/305/300` need a wired Arduino. (c) Subsidiary dists (RPi::I2C/SPI/LCD/ADC/…) not checked out — enumerate per-repo call sites on the Pi (separate). (d) **Pre-existing, orthogonal:** `t/106` + `t/200-202` each fail 17 `rpi_check_pin_status` "set back to default mode" assertions — RPiTest's hardcoded `rpi_default_pin_config` doesn't match this Pi5/RP1's actual default alt-modes; reproduced with a bare `setup_gpio`+`get_alt` (BCM 2=0, 14=31, 15=31, 23=1), no WiringPi::API involvement (user owns these). No wiringpi-api Changes entry — V33 changed no wiringpi-api code (the downstream prereq/Changes live in rpi-wiringpi). Also noticed a pre-existing downstream warning ("Possible precedence problem between ! and string eq" at `RPi/WiringPi/Core.pm:275`) — left untouched (not in scope, user-owned test/code).

- V26: `serialGets` (F13, CRITICAL) — ✅ 2026-06-05 attempt 1: PASS. Fixed the heap buffer overflow: the Perl `serial_gets` passed `my $buf = ""` (0-length) which the XS `serialGets` then wrote up to `nbytes` into. Rewrote `serialGets` as a self-allocating PPCODE XSUB (`Newx`/`Safefree`, `ST(0)=sv_2mortal(newSVpvn(buf,got))`) taking `(fd, nbytes)`; `serial_gets` drops the 0-length buffer + `unpack "A*"` and validates its args. **Read semantics settled with user (2026-06-05): read-up-to-nbytes honoring the port's VTIME timeout, binary-safe, partial-on-timeout/EOF.** Key finding: wiringPi's `serialOpen` sets `O_NONBLOCK`, which defeats VTIME and makes the old loop `croak` on EAGAIN — so the XSUB now clears `O_NONBLOCK` so VTIME governs, retries on EINTR, breaks on 0 (timeout/EOF), and croaks only on a real error (replacing the old `exit(-1)`). Verified on `rpi1` via pipe-backed exercises (real serial device absent — `test/serial_pi.pl` opens `/dev/ttyAMA0`): full read len=11 with embedded NUL + trailing space preserved; partial-on-EOF len=5; neg-nbytes and undef-fd croak; `valgrind --leak-check=full` = 0 bytes lost, no invalid read/write across 200 iters + partial path. `make test` 230 PASS; podchecker clean. **Added a `serial_gets` POD section** (was exported but undocumented). **Downstream (user-directed, fixed same-task):** the identical bug lives in the independent **RPi::Serial** (`~/repos/rpi-serial`, its own `Serial.xs`/`gets`, worse — `exit(-1)`); applied the same fix (`tty_gets` self-allocating PPCODE XSUB, binary-safe `gets`, clear O_NONBLOCK, croak), updated its `gets` POD, added a Changes `3.02 UNREL` section + bumped `$VERSION` 3.01→3.02, rebuilt + `t/00-load.t` PASS. RPi::WiringPi reaches serial only via `$pi->serial` → RPi::Serial (prereq `RPi::Serial => 3.00`); its FAQ.pod serial section just delegates to RPi::Serial (no behavior claims) so no transfer needed; WiringPi::API's own `serial_gets` is NOT used downstream. Pre-existing `rpi-serial t/serial_rx.t` failure (opens a nonexistent port + calls undefined `rx()`) left untouched per user. ⚠ consumer-facing: `serial_gets`/`gets` return is now binary-safe (trailing whitespace/NULs no longer stripped). All changes uncommitted (user commits).

- V27: `spiDataRW` (F14/F15/F16) — ✅ 2026-06-05 attempt 1: PASS. Rewrote the custom SPI marshalling glue. **F14:** each `av_fetch()` is now NULL-checked (and `!SvOK`) so a sparse/undef element in the data aref croaks cleanly instead of dereferencing NULL and crashing. **F15:** each byte is converted with one `SvNV` (was twice). **F16:** replaced the `void spiDataRW(int,SV*,int)` plain-C function (which used `inline_stack_*` macros) + the XS wrapper's `dXSARGS`-in-plain-C / `PL_markstack_ptr++` juggling with a single idiomatic PPCODE XSUB (`EXTEND`/`PUSHs`, `Safefree` before every croak). (F5's VLA→`Newx` and `exit(1)`→`croak` were already done in V21.) **Contract preserved exactly:** `spiDataRW($channel, $aref, $len)` still returns a list of `$len` bytes read — required because **RPi::SPI consumes it directly** (`use WiringPi::API qw(:wiringPi)`; `@$buf = spiDataRW($self->_channel, $buf, $len)`). Verified on `rpi1`: all validation paths croak not crash (sparse aref, undef elem, out-of-range byte, bad channel, len mismatch, non-aref); `valgrind --leak-check=full` = 0 bytes lost, no invalid read/write across 200 iters of the allocate-then-croak paths (F14 + Safefree-before-croak proven); `make test` 230 PASS; podchecker clean. **Success/round-trip path NOT exercised here** — wiringPi's `wiringPiSPISetup` opens `/dev/spidev0.0`, but this Pi5/RP1 only exposes `/dev/spidev10.0`, so setup aborts (same class of device-node limitation as i2c B10) → tracked as **B11**. **Downstream (user-directed):** RPi::SPI (`~/repos/rpi-spi`, freshly cloned, branch master, v2.3611) is a pure consumer (no own XS); it still imports `spiDataRW` from the rebuilt module and `t/00-load.t` PASS — **no code change needed** (contract preserved). RPi::WiringPi reaches SPI only via `$pi->spi` → RPi::SPI (prereq `RPi::SPI => 2.3609`); its FAQ.pod SPI section delegates to RPi::SPI (no behavior claims), so no transfer. spi_data POD behavior description unchanged (still "returns a list of the same length"). ⚠ consumer-facing: a sparse/undef data aref now croaks instead of crashing; valid-input behavior identical. All changes uncommitted (user commits).

## Archived Fixes

- B8: Pre-existing POD errors in `lib/WiringPi/API.pm` — ✅ RESOLVED 2026-06-04 during **isr-migration.md V7**'s POD sweep. Fixed the leading space on `=head1 ADC FUNCTIONS` (which broke the `L</ADC FUNCTIONS>` link), corrected `L</digitalReadByte>` → `L</digitalReadByte()>` to match its `=head2` target, and removed the three whitespace-only lines. `podchecker lib/WiringPi/API.pm` now reports "pod syntax OK" (0 errors, 0 warnings). B8 slot retired.
