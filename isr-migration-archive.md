# Archive: Migrate interrupts to `wiringPiISR2` via a self-pipe

Completed tasks split out of isr-migration.md to keep the active plan lean. See
that file for the live Validation Table, rules and tracking.

## Archived V Tasks

- V1: Prereq gate — confirm installed `<wiringPi.h>` declares `wiringPiISR2` / `wiringPiISRStop` / `struct WPIWfiStatus` (≥ 3.16) — ✅ 2026-06-04 attempt 1: PASS. Grepped `/usr/local/include/wiringPi.h` (the installed 3.18 header on `rpi1`; `/usr/include/wiringPi.h` is absent): `struct WPIWfiStatus` at :298, `wiringPiISR2` at :308 (tagged `// V3.16`), `wiringPiISRStop` at :309 (`//V3.2`). The `wiringPiISR2` prototype confirms the `void* userdata` parameter the self-pipe design keys on: `int wiringPiISR2(int pin, int edgeMode, void (*function)(struct WPIWfiStatus wfiStatus, void* userdata), unsigned long debounce_period_us, void* userdata)`. (`waitForInterrupt2` at :307 and the legacy `waitForInterruptClose` alias at :310 also present; per plan we wrap only `wiringPiISRStop`.) Precheck only — no code change.

- V2: Edge validation + constants (F23) — ✅ 2026-06-04 attempt 1: PASS. Added the four `INT_EDGE_*` constants (`use constant`: SETUP=0, FALLING=1, RISING=2, BOTH=3 — mirror wiringPi's `#define`s) and exported them via `@wpi_constants` → `:constants`/`:all`. Added ordered validation to the Perl `set_interrupt($pin, $edge, $callback)`: `$pin` positive integer, then `$edge` ∈ {1,2,3} (rejects SETUP=0 and junk — SETUP is a setup-only mode, not a trigger), then `$callback` a CODE ref; each croaks with a named message. (Validation placed in the Perl layer, not the soon-to-be-replaced XS `setInterrupt`; the XS keeps its own pin/callback checks as a backstop until V4.) `perl -c` OK; built clean on `rpi1` (no warnings); functional check: constants import as 0/1/2/3; edges 0/4/"x"/undef/1.5/-1 all rejected, bad pin/callback rejected in order. Changes entry deferred to V8 per this plan's maintenance model (V8 updates `3.1801 UNREL`). Discovery logged: examples/POD use `EDGE_RISING` vs the `INT_EDGE_*` chosen here — reconcile in V7/V8.

- V3: Wrap `wiringPiISRStop(int pin)` — ✅ 2026-06-04 attempt 1: PASS. Added a thin XS wrap (`int wiringPiISRStop(pin) / int pin`) in the `# custom` interrupt section right after `setInterrupt` (API.xs), and added `wiringPiISRStop` to `@wpi_c_functions` (→ `:wiringPi`/`:all`). Only `wiringPiISRStop` wrapped — the legacy `waitForInterruptClose` alias is left out per plan. XS `process_file` parses; `perl -c` OK; built clean on `rpi1` (no warnings → symbol resolves against libwiringPi under default linking). Verified: imports + present as an XS sub in WiringPi::API + exported via `:wiringPi`; calling it with no `setup()` reaches wiringPi's own "not initialised" abort (proves the real symbol is invoked). Changes entry deferred to V8 per this plan's model. Needed for V4 re-arm + V6 teardown.

- V4: Self-pipe core (F12/F10/F22) + remove dead initThread — ✅ 2026-06-04 attempt 1: PASS. **Added** (API.xs C section): `isr_event_t {int pin; int edge; long long ts_us;}`; lazily-created non-blocking self-pipe `interrupt_pipe[2]`; `interrupts_dropped` counter (F24, atomic `__sync_fetch_and_add`); `isr2_writer(WPIWfiStatus, userdata)` — async-safe, pin from `userdata` (NOT `wfiStatus.pinBCM`), `write()`s the record, bumps the drop counter on a short/failed write, never touches Perl; `ensure_interrupt_pipe()`; `_arm_interrupt(pin, edge, debounce)` — bounds-checks pin, creates the pipe, **unconditionally `wiringPiISRStop(pin)` then `wiringPiISR2(pin, edge, isr2_writer, debounce, (void*)(intptr_t)pin)`** (the stop-first kills F25 stacked listeners; confirmed safe on an unarmed pin — wiringPiISRStop only aborts pre-`setup()`, else no-ops); `interrupt_fd()` (read end, -1 until first arm). New XSUBs `_arm_interrupt`/`interrupt_fd`; added `#include <unistd.h>`/`<fcntl.h>`. **Deleted**: the 40 generated `interruptHandler_*` trampolines + `MAKE_HANDLER`/`APPLY_TO_PINS`/`HANDLER_PTR`/`interrupt_handlers[]`; `mine`; `perl_callbacks[]`; the `event_queue`+mutex+cond+dispatcher thread + `ISR_enqueue/dequeue/dispatcher_main`; the XS `setInterrupt` (C fn + XSUB) and its `@wpi_c_functions` export + API.h decl; and (user-authorized, resolves the shared-`mine` entanglement) the dead unexposed `initThread` + `thread_callback_sv` + `PI_THREAD(myThread)` + API.h decl — **this completes threads-patch.md V1's deletion early**. Perl: `set_interrupt` now arms via `_arm_interrupt($pin,$edge,0)` (callback-registry + dispatch land in V5 — noted in a code comment); `interrupt_fd` exported via `@wpi_perl_functions`. XS parses; `perl -c` OK; residue grep empty; clean `realclean`+build on `rpi1` (no warnings; `wiringPiISR2`/`wiringPiISRStop` resolve); `make test` PASS (`.so` loads under bind-now — F22 check); verified `interrupt_fd` importable + returns -1 pre-arm, `_arm_interrupt` present, `setInterrupt`/`initThread` gone. Changes deferred to V8. **Note for threads-patch.md:** its V1 is now done here — mark it accordingly when that plan resumes.

- V5: Perl-side dispatch — ✅ 2026-06-04 attempt 1: PASS. `set_interrupt($pin,$edge,$cb)` now stores `$cb` in the per-interpreter file-lexical `%_interrupt_cb` (keyed by the user's pin) before `_arm_interrupt`. Added `dispatch_interrupts()` — non-blocking drain: reads 16-byte records from a cached dup (`<&`) of `interrupt_fd()`, `unpack "i i q"` → `{pin,edge,ts_us}`, calls `$_interrupt_cb{$pin}->($edge,$ts_us)`; drain contract honored (`undef`+EINTR→retry, else stop; `n==0` EOF→stop; never busy-spins on EAGAIN) — returns the dispatched count. Added `wait_interrupts($timeout_ms)` — 4-arg `select` on the fd (ms→s; undef=block) then dispatch. Added XS `interrupt_dropped()` accessor (+ C accessor) returning the `interrupts_dropped` counter (F24). New private `_interrupt_fh` caches the dup keyed on fd (re-opens if the pipe is torn down + re-armed; dup means closing it never closes the C-side fd). Exports: `dispatch_interrupts`/`wait_interrupts`/`interrupt_dropped` added to `@wpi_perl_functions` (→ `:perl`/`:all`). XS parses; `perl -c` OK; clean build (no warnings); **hardware-free end-to-end test** (inject `pack "i i q"` records into a non-blocking pipe, stub `interrupt_fd`/`_arm_interrupt`): 3 records routed to the correct pin-7/pin-9 callbacks with correct edge+ts, second drain returns 0 (no spin), `wait_interrupts(500)` select+dispatch works, `interrupt_dropped`=0; `make test` PASS under bind-now. Changes updated (kept current per user). Callback registry in Perl = F17/F18/F19 stay dissolved.

## Archived Fixes
